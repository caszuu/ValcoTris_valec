#pragma once

const int _cfg_width = 12; // screen width - make sure it is the same as the m_width in the library
const int _cfg_height = 10; // screen height - make sure it is the same as the m_height in the library
const bool _cfg_scroll_enable = true; // true | false
const bool _cfg_overflow_enable = (_cfg_scroll_enable || false); // true | false 
const bool _cfg_line_animation = true; // true | false
const int _cfg_joy_dead = 512;
const int _cfg_joy_res = 4096;